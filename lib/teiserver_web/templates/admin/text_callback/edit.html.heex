<% bsname = view_colour() %>

<div class="row section-menu">
  <div class="col-md-12">
    <div class={"card border-#{bsname}"}>
      <div class="card-body">
        <TeiserverWeb.Components.Admin.TextCallbackComponents.section_menu
          active="edit"
          colour={bsname}
        />

        <hr />

        {render(
          "form.html",
          Map.put(assigns, :action, ~p"/admin/text_callbacks/#{@text_callback}")
        )}

        <hr />
        {link(raw("<i class='far fa-fw fa-trash'></i> Delete"),
          to: ~p"/admin/text_callbacks/#{@text_callback}",
          method: "delete",
          data: [confirm: "Are you sure?"],
          class: "btn btn-danger btn-outline"
        )}
      </div>
    </div>
  </div>
</div>
